You are implementing set-piece batch 3 (step 10a) of a procedural street-building system in a Godot 4.7 GDScript project at C:\Users\Traff\Documents\van-gunner (Windows; `py -3`). Read first: `scripts/travel/facades/facade_set_piece.gd`, `facade_set_pieces.gd`, `corridor_facades.gd`, `facade_mesh_kit.gd`, `facade_materials.gd`, `facade_signs.gd`, `facade_props_ground.gd`, two existing pieces as templates (`set_pieces/collapsed_block.gd`, `set_pieces/rooftop_billboard.gd` and their `.tres`), and `scenes/corridor/facade_surface.gdshader`'s uniform block (top 40 lines) — styles: 3 glass curtain, 6 bare frame; `overgrowth`, `collapse_y`, `lit_ratio`, `windows_on`, `emission_energy`, `ground_kind` (1 storefront, 4 dock).

# Project rules
GDScript: tabs, LF, ~100 columns, two blank lines between functions, typed everything, `&"..."` StringNames, `##` docs; comments explain why. Subclasses `extends FacadeSetPiece`, no `class_name`, <= 120 lines each. Every box gated through the mesh kit; quads only via `facade_body.add_quad`. Lights: `OmniLight3D`, `shadow_enabled = false`, `light_cull_mask = 1`, `visibility_range_end = 60`, group `&"facade_lights"`, skipped at the world cap (12). Never launch Godot with a window; never open `.png`/`.import`/`.godot/`. Deliver `.gd.uid` files. Conventions: `x_face`, out = `-side_sign`, u -> z, `y0 = BASE_Y`, `y_top = y0 + plan.height`, roof plate 1.2 m deep behind the face.

# Pieces (script + .tres each; `.tres` follows the existing files)
1. `parking_deck` (weight 2, districts [industrial, commercial]). `pick_plan`: widest plan with width >= 10 and height >= 14. `apply_plans`: `params.style = 6` (bare frame), `params.windows_on = 0.0`, `params.lit_ratio = 0.0`, `params.base_color = Color(0.32, 0.32, 0.31)`, `params.floor_height = 3.0`, `params.ground_height = 3.0`, `params.ground_kind = 0`; `plan.rare = id`. `build`: per deck level k (y = `y0 + 3.0 + k * 3.0` for k in 0..floors-1 while below `y_top - 1.0`): a waist band box `(0.3, 0.5, plan.width - 1.0)` protruding 0.3 at y level + 0.9 (concrete, mesh "DeckBands"); a fluorescent strip box `(0.15, 0.08, plan.width - 2.0)` 0.1 off the face under the slab at y level + 2.6 with `prop_material(&"fluoro", Color(0.8, 0.9, 0.95), 0.4, 0.1, Color(0.75, 0.9, 1.0), 2.0)` on 70 % of levels (mesh "DeckLights"); a ramp stripe: a yellow-black box `(0.02, 0.15, 3.0)` at the ground level (`prop_material(&"hazard", Color(0.9, 0.75, 0.1), 0.8, 0.0)`), one per plan at u = 2.0.
2. `overgrown_ruin` (weight 2, districts [derelict]). `pick_plan`: widest. `apply_plans`: `params.overgrowth = 0.7`, `params.collapse_y = plan.height * 0.6`, `params.lit_ratio = 0.0`, `params.broken_ratio = 0.5`, `params.damage = 0.5`; `plan.rare = id`. `build`: 4-8 vine boxes hanging from window sills: `(0.06, rng(1.0, 3.0), 0.5)` 0.04 off the face below random windows (`prop_material(&"vine", Color(0.08, 0.14, 0.05), 0.95, 0.0)`, mesh "Vines"); 2-3 saplings on the roofline just below the collapse edge: trunk `(0.1, 1.2, 0.1)` + crown `(0.8, 0.9, 0.8)` (`prop_material(&"sapling", Color(0.12, 0.22, 0.08), 0.95, 0.0)`), at x_face + side_sign * 0.5, y = `y0 + params.collapse_y` (mesh "Saplings"). Gate everything.
3. `glass_crown` (weight 3, districts [commercial]). `pick_plan`: widest. `apply_plans`: `params.style = 3`, `plan.height = 40.0` (also `params.facade_height = 40.0`, `plan.floors` recomputed), `params.lit_ratio = 0.45`, `params.lit_color = Color(0.8, 0.88, 1.0)`; `plan.rare = id`. `build`: crown band: box `(0.3, 2.0, plan.width)` protruding 0.3 at y `y0 + 38.5` with `prop_material(&"crown_glow", Color(0.5, 0.7, 0.9), 0.3, 0.2, Color(0.6, 0.8, 1.0), 2.4)` (mesh "Crown"); beacon `(0.3, 0.3, 0.3)` at the roof centre with `prop_material(&"beacon_red", ...)` (the key exists from `antenna_farm`; use the same key/values) on a mast `(0.1, 3.0, 0.1)` (mesh "CrownBeacon").
4. `radio_mast` (weight 3, districts [industrial, derelict]). `pick_plan`: lowest plan with height <= 30. `build` on the roof at u mid, x = x_face + side_sign * 0.7: lattice mast 15 m: three legs `(0.08, 15.0, 0.08)` at the corners of a 0.6 triangle, rings every 2.5 m as three horizontal `(0.08, 0.08, 0.6)` / rotated boxes (use `add_box_node` with rotation y for the two slanted ones), iron, mesh "Mast" + nodes "MastRing%d"; two guy-line boxes `(0.02, 12.0, 0.02)` rotated z +-0.35 from the mast top down to the roof (add_box_node, iron); beacon at the top (`beacon_red`).
5. `blown_out_shop` (weight 3, districts [tenement, commercial]). `pick_plan`: widest plan with `ground_kind == 1` (`can_apply` false otherwise). `apply_plans`: `params.ground_kind = 2` (boarded look on the shader? No — keep 1) ... set `params.emission_energy = 0.0` for the ground zone is impossible per-zone; instead set `params.lit_ratio = 0.05` and `params.soot = 0.8`; `plan.rare = id`. `build`: black-out panel over the first unit's glass: box `(0.02, 2.9, unit_w - 1.0)` 0.03 off the face at y `y0 + 1.95` with `prop_material(&"charred", Color(0.02, 0.02, 0.02), 0.95, 0.0)` (mesh "Charred"); 3 debris boxes `(0.4..0.8)` random yaw on the strip (x 8.25) within that unit (gated, `rubble` material, mesh "ShopDebris"); police tape: box `(0.02, 0.08, unit_w)` at x `side_sign * 7.75`, y `y0 + 1.2`, with `prop_material(&"tape", Color(0.95, 0.85, 0.1), 0.6, 0.0)` (mesh "Tape"); two tape posts `(0.05, 1.2, 0.05)` at its ends, iron.
6. `gas_canopy` (weight 3, districts [industrial, commercial], lights 1). `pick_plan`: widest plan with width >= 10. `apply_plans`: `plan.height = 8.0` (`params.facade_height = 8.0`, floors 1), `params.windows_on = 0.0`, `params.ground_kind = 1`, `params.ground_units = 1.0`; `plan.rare = id`. `build`: canopy slab `(3.0, 0.5, 8.0)` centred at x `x_face - side_sign * 1.5` (spans x 7.3..8.8 on the right) hmm — that intrudes the lane box only if below y 6: its bottom is `y0 + 6.2` = 5.8 in tile y, which is BELOW `LANE_TOP_Y` 6.0 and it crosses abs(x) < 7.6 -> rejected. So put the canopy at y bottom `y0 + 6.6` (tile 6.2): centre y `y0 + 6.85`; then the underside strip: box `(2.8, 0.05, 7.8)` under it with `prop_material(&"canopy_glow", Color(0.9, 0.9, 0.85), 0.5, 0.0, Color(1.0, 1.0, 0.9), 2.2)`; two posts `(0.3, 6.6, 0.3)` at x `side_sign * 8.0`, u = mid +- 3.0, from y0 (they stand on the strip at abs(x) 7.85..8.15: passes); two pumps `(0.5, 1.2, 0.4)` at x `side_sign * 8.2`, u = mid +- 1.5 (`prop_material(&"pump", Color(0.7, 0.1, 0.1), 0.6, 0.2)`); price pole `(0.15, 7.0, 0.15)` at u = 1.0, x `side_sign * 8.3`, with a sign panel `(0.1, 1.2, 0.8)` at its top using `sign_material("0.95", 3, false, Color(1, 0.9, 0.7), 2.0, 0, 0.0, 0.0, 0.0, 4.0)`; one `OmniLight3D` under the canopy (white, energy 1.5, range 10). All gated.

# `.tres` files
One per piece in `resources/facades/set_pieces/`.

# Do not touch
Shaders, `facade_body.gd`, `facade_keep_out.gd`, `facade_plan.gd`, `corridor_segment.*`, `travel_*.gd`, `van.tscn`, balance/export files, baselines.

# Verification
`py -3 tools/check.py` clean; `py -3 tools/smoke.py` passes (fingerprint unchanged; `OK stress` if the stress step exists yet, and `bay mouth clear:`). One-off headless sanity like step 8's: for each new id find a seed whose `rare_id()` matches, print the nodes added, then `open_bay` on that side and assert the rare is gone. Report check tail, smoke tail (30 lines), sanity output, `wc -l`, `git status --short`. Do not commit.
