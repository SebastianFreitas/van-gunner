You are implementing step 10b of a procedural street-building system in a Godot 4.7 GDScript project at C:\Users\Traff\Documents\van-gunner (Windows; `py -3`): the last four set-pieces (chapel, cinema_marquee with a marquee shader, crane_site, searchlight) and the industrial "overhead" district dressing (pipe bridges, catwalks, ribs) that brings back the old corridor's cross-street flavour without ever covering a bay. Read first: `scripts/travel/facades/facade_set_piece.gd`, `facade_set_pieces.gd`, `corridor_facades.gd` (how `Facades/Span` is built for span pieces), `facade_mesh_kit.gd`, `facade_materials.gd`, `facade_signs.gd`, `facade_district.gd`, two existing pieces (`set_pieces/pipe_bridge.gd` as the span template, `set_pieces/rooftop_billboard.gd` for the two-surface text panel), `scenes/corridor/facade_sign.gdshader`, and `resources/facades/districts/industrial.tres`.

# Project rules
GDScript: tabs, LF, ~100 columns, two blank lines between functions, typed everything, `&"..."` StringNames, `##` docs; comments explain why. Set-piece subclasses `extends FacadeSetPiece`, no `class_name`, <= 120 lines; helpers `RefCounted`, no `class_name`. Every box gated; quads only via `facade_body.add_quad`; lights as in the other pieces (no shadows, cull mask 1, group `facade_lights`, world cap 12). A node script is allowed only for the searchlight rotation (a 12-line `Node3D` script, no `class_name`). Never launch Godot with a window; never open `.png`/`.import`/`.godot/`. Deliver `.gd.uid` files.

# 1. `scenes/corridor/facade_marquee.gdshader` (CREATE, ~40 lines)
Spatial, unshaded-ish: `uniform vec3 color : source_color = vec3(1.0, 0.85, 0.5); uniform float energy = 3.0; uniform float bulbs_per_m = 6.0; uniform float length_m = 8.0; uniform float speed = 6.0;` Fragment: bulb grid along UV.x: `float u = UV.x * length_m * bulbs_per_m; float cell = floor(u); vec2 c = vec2(fract(u) - 0.5, UV.y - 0.5); float bulb = 1.0 - smoothstep(0.22, 0.3, length(c)); float chase = step(0.5, fract((cell - TIME * speed) / 3.0));` (every third bulb lit, chasing) `ALBEDO = vec3(0.05); EMISSION = color * energy * bulb * chase; ROUGHNESS = 0.5;`.
`facade_materials.gd` (EDIT): `static func marquee_material(color: Color, length_m: float) -> ShaderMaterial` (lazy-loaded shader, sets `color` and `length_m`).

# 2. Pieces
a. `chapel` (weight 2, districts [tenement, civic]). `pick_plan`: widest plan with width >= 10. `apply_plans`: `params.style = 5` (stone), `params.window_h = 2.8`, `params.window_pitch = 3.4`, `params.lit_ratio = 0.3`, `params.lit_color = Color(1.0, 0.75, 0.45)`, `params.ground_kind = 5` (arcade), `params.band_every = 0.0`, `plan.height = maxf(plan.height, 18.0)` (+ `facade_height`, floors); `plan.rare = id`. `build`: rose window: a disc = cylinder via `add_cylinder_node` (r 1.5, h 0.1, rotated z = PI/2 so it faces the road) at u mid, y `y0 + 9.5`, 0.06 off the face, material `prop_material(&"stained_glass", Color(0.4, 0.15, 0.25), 0.4, 0.1, Color(0.9, 0.5, 0.35), 2.2)`; a ring around it `(0.12, 3.4, 3.4)`? Use 8 short boxes around the disc edge (`trim_material`) — or simpler a slightly larger disc behind it (r 1.7, h 0.06, trim material); bell tower: a box `(1.4, 34.0 - plan.height, 6.0)`? The tower is a separate building block ABOVE the roof: box `(1.6, rng(8.0, 14.0), 6.0)` centred u = width - 4.0 (one end), x = x_face + side_sign * 0.8, standing on the roof (`trim_material`, shadows ON, mesh "Tower"); a clock face on its road side: cylinder r 1.0 h 0.06 at 0.9 up from the tower top... at tower centre height, 0.04 off the tower's road face, `prop_material(&"clock_face", Color(0.9, 0.88, 0.8), 0.6, 0.0, Color(1.0, 0.95, 0.8), 1.4)`, two hands as boxes `(0.02, 0.6, 0.06)` and `(0.02, 0.4, 0.06)` rotated z by rng angles (iron); a pointed cap on the tower: cylinder top 0.05 bottom 1.3 h 2.0 (metal grey).
b. `cinema_marquee` (weight 3, districts [commercial], lights 1). `pick_plan`: widest plan with width >= 10 and `ground_kind == 1`. `apply_plans`: `params.lit_ratio = 0.6`; `plan.rare = id`. `build`: canopy `(2.0, 1.0, 8.0)` protruding 2.0 with its bottom at `y0 + 6.6` (tile 6.2, above the lane box top 6.0; abs(x) reaches 6.8 which is inside the lane x range, so the height is what makes it pass — state that in the report), `trim_material`, shadows ON, mesh "Marquee"; on its three outer vertical faces (road side and both ends) a bulb strip box `(0.02, 0.25, 8.0)` / `(2.0, 0.25, 0.02)` with `marquee_material(Color(1.0, 0.85, 0.5), 8.0)` (nodes "MarqueeBulbs*"); a title panel on the road face: two-surface box `(0.1, 0.5, 7.0)` at the canopy's centre height with `sign_material(word, 4, false, Color(1.0, 0.95, 0.85), 2.0, 0, seed, 0.0, 0.0, 8.0)` and `const TITLES := ["NOW SHOWING", "LAST NIGHT", "DOUBLE BILL", "MIDNIGHT", "SOLD OUT", "NO REFUNDS"]`; a vertical "CINEMA" blade above the canopy via `facade_signs.build_blade` (size `(1.2, 5.0, 0.3)`, bottom `y0 + 8.0`, cyan, dead 0.0, flicker 0.1); one white `OmniLight3D` under the canopy (energy 1.8, range 9) at `(x_face - side_sign * 1.2, y0 + 6.4, z_mid)` — its position box `(0.2, 0.2, 0.2)` sits at tile y 6.0..6.2 hmm: y0 + 6.4 = 6.0 tile -> the box spans 5.9..6.1 and intersects the lane top; use `y0 + 6.7`.
c. `crane_site` (weight 2, districts [industrial, commercial, derelict]). `pick_plan`: widest plan with width >= 10. `apply_plans`: `params.style = 6` (bare frame), `params.windows_on = 0.0`, `params.lit_ratio = 0.0`, `params.ground_kind = 0`, `plan.height = clampf(plan.height, 16.0, 28.0)` (+ facade_height, floors); `plan.rare = id`. `build`: green safety net over floors 1-3: box `(0.02, 9.6, plan.width - 1.0)` at x `side_sign * 8.2` (transparent `net` material from `scaffolded` — same key), mesh "CraneNet"; tower crane: mast `(1.2, 44.0 - plan.height, 1.2)`? No: the mast stands behind the face: base at the roof (`y_top`), x = x_face + side_sign * 1.0, u = width - 3.0, height = `44.0 - plan.height` (top at tile y 43.6), iron lattice look = box plus 6 cross boxes (ungated after the mast passes), mesh "CraneMast"; jib: box `(14.0, 0.6, 0.8)`... the jib crosses the street: it spans from the mast over the road: box `(14.0, 0.8, 0.8)` centred at x `x_face - side_sign * 6.0` (i.e. reaching to x ~1.8 past the centre? from 8.8 -> 8.8-6-7 = -4.2; fine: it is at y 42 far above everything), y `y_top + (44.0 - plan.height) - 0.4`, rotated y by rng(-0.3, 0.3) (add_box_node), material `prop_material(&"crane_yellow", Color(0.85, 0.6, 0.1), 0.6, 0.3)`, node "CraneJib"; a counter-jib `(5.0, 0.8, 0.8)` on the other side of the mast; a hook cable `(0.03, 12.0, 0.03)` hanging from the jib's outer third (iron) with a hook block `(0.4, 0.6, 0.4)`; beacon at the mast top (`beacon_red`). Every piece must satisfy the gate (all above y 6 except the net, which is at abs(x) 8.2).
d. `searchlight` (weight 2, districts [commercial, civic]). `pick_plan`: lowest plan with height <= 34. `build`: pedestal `(1.0, 1.0, 1.0)` on the roof at u mid, x = x_face + side_sign * 0.6 (concrete); a housing cylinder r 0.5 h 0.8 (metal grey); the beam: a `MeshInstance3D` with a `CylinderMesh` (top_radius 0.3, bottom_radius 4.0, height 30.0) whose material is `StandardMaterial3D` with `shading_mode` UNSHADED, `transparency` ALPHA, `blend_mode` ADD, `albedo_color = Color(0.9, 0.9, 1.0, 0.12)`, `cull_mode` DISABLED, `cast_shadow` OFF (cache it in `facade_materials` as `beam_material()`); position the beam so its narrow end sits on the housing and it points up and 35 degrees outward over the street (`rotation = Vector3(0, 0, side_sign * 0.6)` after moving its origin: CylinderMesh is centred, so offset by 15 m along its axis — compute with a small parent `Node3D` "SearchlightPivot" at the housing top: the beam is its child at local position (0, 15, 0) with the cylinder's narrow end... CylinderMesh top_radius is the +Y end; we want the narrow end at the pivot, so top_radius 0.3 bottom 4.0 means the wide end is at -Y: rotate the beam child by PI about X (or swap radii: top 4.0 bottom 0.3 and place it at +15 with no flip). Use top 4.0 / bottom 0.3 at local (0, 15, 0). Then attach `scripts/travel/facades/set_pieces/searchlight_pivot.gd` (a `Node3D` script, no class_name, ~12 lines: `@export var degrees_per_second := 20.0`; `_process`: `rotate_y(deg_to_rad(degrees_per_second) * delta)`; tilt is set once in `_ready` as `rotation.z = 0.6`) to the pivot. The beam's AABB (30 m tall, 8 m wide, tilted) is gated with `box_aabb(center, Vector3(30, 30, 30))` conservative: it starts at roof height so it passes; skip the piece if the roof is above 34 m.

# 3. Industrial overheads (district dressing, not a rare)
`scripts/travel/facades/facade_overheads.gd` (CREATE, RefCounted, static, <= 200 lines):
```
extends RefCounted
## Cross-street industrial dressing (pipe bridges, catwalks, ribs) built under Facades/Overhead
## when both sides are plain; it never reaches below 9 m over the road, so a bay mouth is never
## touched, and it is dropped the moment either side opens.
```
- `static func build(host: Node3D, plans_left: Array, plans_right: Array, keep_out: RefCounted, rng: RandomNumberGenerator, district: FacadeDistrict) -> void` — only when `district.overhead_chance` (ADD `@export var overhead_chance := 0.0` to `facade_district.gd` and set it to 0.55 in `industrial.tres`, 0.15 in `derelict.tres`; others 0.0) passes `rng.randf()`. Pick one kind (rng): (a) pipe bridge: two pipes (cylinders rotated to lie along X, r 0.35, length 18.4) at y `10.5` and `11.6` tile-space, z = rng(-7, 7), `rust_pipe_material`, with four hanger straps up to 13.5 (iron); (b) catwalk: floor `(18.4, 0.15, 2.0)` at y 9.6, rails: two top rails `(18.4, 0.05, 0.05)` at y 10.7 on both edges + posts every 2.0 m `(0.05, 1.1, 0.05)`, iron; (c) truss: two chords `(18.4, 0.4, 0.4)` at y 12.0 and 13.4 with 8 diagonal boxes `(0.15, 1.6, 0.15)` rotated z +-0.6 between them (add_box_node), iron; (d) ribs: three portal frames: per frame two posts on the sidewalk strips? NO posts (they would stand on the strip below 6 m at the bay side... they are allowed on NONE sides at abs(x) 7.8..8.6, but overheads are dropped when a side opens, so posts are fine): two posts `(0.5, 12.0, 0.5)` at x +-8.3 from y 0 (gated per side keep-out! use the right keep-out for x > 0 and build a left keep-out `FacadeKeepOut.new(-1.0, 0)` for x < 0) and a beam `(18.0, 0.5, 0.55)` at y 12.0, `trim_material(&"concrete_grey")`... use `concrete_material()`; frames at z -7, 0, 7. Every box gated with the keep-out of the side its x falls on (both are NONE here, so only the lane box matters: nothing below y 6 at abs(x) < 7.6).
- `corridor_facades.gd` (EDIT): after both sides are built in `configure` (and after a span rare, if any), if `_openings == [0, 0]` and no span rare: `_build_overhead()` -> `Facades/Overhead` root with `_FacadeOverheads.build(...)` using an RNG seeded `hash([seed, &"overhead"])`; `set_opening` to a non-NONE opening frees `Facades/Overhead` (like `Span`). Visibility post-pass on it.

# `.tres` files
One per piece in `resources/facades/set_pieces/`; district `.tres` edits as stated.

# Do not touch
`facade_surface.gdshader`, `facade_sign.gdshader`, `facade_body.gd`, `facade_keep_out.gd`, `facade_plan.gd`, `corridor_segment.tscn`, `travel_*.gd`, `van.tscn`, balance/export files, baselines.

# Verification
`py -3 tools/check.py` clean (the marquee shader compiles here); `py -3 tools/smoke.py` passes (fingerprint unchanged; `OK stress`/`bay mouth clear:` lines present as applicable). One-off headless sanity like step 8's for the four ids, plus: configure an industrial tile with seeds until `Facades/Overhead` exists, print its children, `open_bay(&"left")` and assert `Overhead` is gone. Report check tail, smoke tail (30 lines), sanity output, `wc -l`, `git status --short`. Do not commit.
