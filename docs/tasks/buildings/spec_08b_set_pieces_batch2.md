You are implementing set-piece batch 2 (step 8b) of a procedural street-building system in a Godot 4.7 GDScript project at C:\Users\Traff\Documents\van-gunner (Windows; `py -3`). The framework exists: `scripts/travel/facades/facade_set_piece.gd` (`class_name FacadeSetPiece`: `id`, `weight`, `districts`, `span`, `lights`; hooks `can_apply`, `pick_plan`, `apply_plans`, `build(ctx)`), `facade_set_pieces.gd` (roll), `facade_registry.gd` (`set_pieces()`), `corridor_facades.gd` (calls the hooks; spans build under `Facades/Span` with `ctx.side_sign == 0.0`, `ctx.plans_left/plans_right`, `ctx.keep_out` = right side's keep-out), the existing pieces in `scripts/travel/facades/set_pieces/` + `resources/facades/set_pieces/` (read `rooftop_billboard.gd`/`.tres` and `water_tower.gd` as templates), `facade_mesh_kit.gd` (`add_box`, `add_box_ungated`, `commit`, `add_box_node`, `add_cylinder_node`), `facade_materials.gd` (`prop_material`, `sign_material`, `label_texture`, `trim_material`, `iron_material`, `metal_grey_material`, `rust_pipe_material`, `concrete_material`, `ground_material`), `facade_signs.gd` (blade builder), `facade_props_upper.gd` (window column/row math: reuse its helpers if public, else copy the two formulas). Read all of these first.

# Project rules
GDScript: tabs, LF, ~100 columns, two blank lines between functions, typed everything, `&"..."` StringNames, `##` docs; comments explain why. Subclasses `extends FacadeSetPiece`, no `class_name`, <= 120 lines each. Every box through the mesh kit (gated) or `add_box_ungated` only for sub-parts of an already-gated box; quads only via `facade_body.add_quad`. Set-pieces never build on a BAY or SIDE_STREET side (the framework guarantees the target side is NONE; spans need both NONE). Lights: `OmniLight3D` with `shadow_enabled = false`, `light_cull_mask = 1`, `visibility_range_end = 60`, group `&"facade_lights"`, and skip the light when `get_tree().get_nodes_in_group(&"facade_lights").size() >= 12`. Never launch Godot with a window; never open `.png`/`.import`/`.godot/`. Deliver `.gd.uid` files.

Geometry conventions: `x_face = FacadePlan.face_x(plan, side_sign)`; out = `-side_sign` on X; u -> z (`z = z0 + u` for side_sign > 0 else `z1 - u`); `y0 = FacadePlan.BASE_Y`; `y_top = y0 + plan.height`; window columns at `u = (col + 0.5) * pitch`, floor line k at `y0 + GROUND_HEIGHT + k * FLOOR_HEIGHT`, pane bottom at floor line + `window_sill`. Read `pitch/window_sill/window_h/windows_on` from `plan.params` with the shader defaults as fallbacks (2.6 / 0.9 / 1.7 / 1.0).

# Pieces (script + .tres each)
1. `burning_tenement` (weight 3, districts [tenement, derelict], lights 1). `pick_plan`: widest plan with `windows_on > 0.5` and style not in [3, 6] (`can_apply` false otherwise). `apply_plans`: on the target: `params.flicker = 1.0`, `params.lit_ratio = 0.55`, `params.lit_color = Color(1.0, 0.45, 0.12)`, `params.soot = 0.9`, `params.boarded_ratio = 0.0`, `params.broken_ratio = 0.25`, `params.emission_energy = 3.0`; `plan.rare = id`; tags += &"burning". `build`: 3-5 flame slabs: boxes `(0.03, 1.1, 0.7)` 0.05 off the face inside random upper windows (floor k >= 2), material `prop_material(&"flame", Color(1.0, 0.5, 0.1), 0.5, 0.0, Color(1.0, 0.45, 0.1), 5.0)`, one mesh "Flames"; 2-3 soot smears are shader-only (nothing to build); one `OmniLight3D` "FireLight": colour (1.0, 0.45, 0.15), energy 2.5, range 12, attenuation 1.3, at `(x_face - side_sign * 1.0, y0 + 9.0, z_mid)` (subject to the cap; gate its position box `(0.2, 0.2, 0.2)` — it passes on a NONE side).
2. `collapsed_block` (weight 3, districts [tenement, industrial, derelict]). `pick_plan`: the plan with the greatest height >= 18 (`can_apply` false if none). `apply_plans`: `params.collapse_y = rng.randf_range(12.0, minf(20.0, plan.height - 4.0))`, `params.damage = 0.6`, `params.broken_ratio = 0.4`, `params.lit_ratio = 0.0`; `plan.rare = id`; tags += &"collapsed". `build`: rubble: 8-14 boxes with random size 0.3..0.9 per axis and random yaw, centre x `side_sign * 8.25`, y = size.y / 2 - 0.06, u random inside the plan (gated: `add_box` with a yaw-expanded AABB: use `FacadeKeepOut.box_aabb(center, size, yaw)` yourself and `add_box_ungated` when it passes; material `prop_material(&"rubble", Color(0.22, 0.21, 0.2), 0.95, 0.0)`) in one mesh "Rubble"; 4 rebar rods via `add_box_node` `(0.03, 1.5, 0.03)` tilted (rotation z +-0.3..0.6, x +-0.3) standing in the rubble, iron; 2-3 slab stubs `(0.8, 0.3, 2.0)` protruding 0.8 at the two floor lines just below `collapse_y` (gated), concrete, mesh "SlabStubs".
3. `neon_blade` (weight 5, districts [tenement, commercial]). `pick_plan`: widest plan with width >= 8. `build`: call the blade builder in `facade_signs.gd` if it exposes one (`build_blade(host, plan, side_sign, keep_out, word, size, bottom_y, color, dead_ratio, flicker_amount)`; if it does not exist, ADD that static function to `facade_signs.gd` by extracting the blade code so both the sign builder and this piece use it) with size `(1.6, 7.0, 0.3)`, bottom `y0 + 6.6`, word from `district.sign_words` (rng), colour from the neon palette (rng), `dead_ratio` 0.15, `flicker_amount` 0.4. `plan.rare = id` in `apply_plans`.
4. `pedestrian_bridge` (weight 3, districts [], span true, lights 0). `build` (ctx.side_sign 0.0, host = Span root, keep_out = right side's): an enclosed bridge across the street: floor `(18.4, 0.25, 3.0)` at y `y0 + 9.0 + 0.125` centred x 0, z = z_mid of the tile (0.0) + rng(-3, 3); roof `(18.4, 0.2, 3.0)` at y `y0 + 12.0`; two side walls `(18.4, 2.6, 0.15)` at z +-1.45, y `y0 + 10.5`, each with a window strip: an emissive quad-box `(18.0, 0.9, 0.02)` on its outer face (`prop_material(&"bridge_glow", Color(0.7, 0.8, 0.9), 0.4, 0.1, Color(0.8, 0.9, 1.0), 1.8)`); two end portals `(0.4, 3.2, 3.2)` at x +-9.2 (meeting the facades), concrete for the shell, mesh "Bridge" + "BridgeGlow". Gate every box with the right keep-out (`lane_box` top is y 6: the bridge bottom at 8.6 passes).
5. `pipe_bridge` (weight 3, districts [industrial], span true). `build`: three pipes crossing: cylinders via `add_cylinder_node` rotated to lie along X (rotation z = PI/2; the kit's `add_cylinder_node` builds vertical cylinders — add an optional `rotation_rad: Vector3 = Vector3.ZERO` parameter to it, AABB computed as `box_aabb(center, Vector3(height, 2*r, 2*r))` when rotated about z), r 0.6, height 18.4, at y `y0 + 10.0`, `+11.5`, `+13.0`, z spread 0 / 1.4 / -1.4 (+ rng jitter), `rust_pipe_material`; two valve wheels: cylinders r 0.5 h 0.1 (rotated to face +-X) at x +-3, on the middle pipe, iron; four hanger straps `(0.1, 1.2, 0.1)` from the top pipe up to y 14.2, iron.
6. `laundry_balconies` (weight 4, districts [tenement]). `pick_plan`: widest plan with windows and style in [0, 2]. `apply_plans`: `plan.rare = id`. `build`: balconies at EVERY floor k >= 1 and every window column (cap 24): slab `(0.9, 0.14, 2.2)` protruding 0.9 at floor line k + window_sill - 0.07 (concrete, mesh "Balconies"); rail: two posts `(0.04, 1.0, 0.04)` + top rail `(0.04, 0.05, 2.2)` + low panel `(0.03, 0.45, 2.2)` at the outer edge (iron, mesh "BalconyRails"); laundry: 2-4 cloth quads per balcony as thin boxes `(0.02, 0.5, 0.35)` hung at the rail top, colours from a palette (white, red, blue, yellow, green; `prop_material("laundry_" + name, colour, 0.95, 0.0)`), meshes per colour (<= 5 nodes). All gated (they protrude 0.9 at abs(x) >= 7.9 above y 4: passes the lane box).
7. `mural` (weight 3, districts [tenement, derelict]). `pick_plan`: widest plan with style in [1, 2] (plaster/concrete). `apply_plans`: `params.windows_on = 0.0` (a blank wall), `params.grime = 0.3`, `plan.rare = id`. `build`: a mural quad: box `(0.02, min(plan.height - 6.0, 14.0), plan.width - 2.0)` 0.02 off the face, bottom at `y0 + 4.8`; material: a `StandardMaterial3D` with `albedo_texture` = a generated `ImageTexture` (256 x 256): fill a base colour, draw 3-5 large filled rectangles and 2-3 circles (set_pixel loops) in a 4-colour palette picked by rng from three palettes (warm: (0.85,0.35,0.2)/(0.95,0.75,0.25)/(0.2,0.25,0.35)/(0.9,0.9,0.85); cool: (0.2,0.5,0.7)/(0.9,0.85,0.7)/(0.1,0.15,0.25)/(0.7,0.2,0.3); mono: (0.1,0.1,0.1)/(0.9,0.9,0.9)/(0.6,0.6,0.6)/(0.8,0.2,0.2)), then one giant glyph word (2-4 letters from the district sign_words, `BlockGlyphs.draw_text` at scale 10-14) and a fade: multiply every pixel by 0.6..1.0 noise; roughness 0.95; cache the texture by `tile_seed` in a static Dictionary on the piece script. Mesh "Mural", shadows OFF.
8. `scaffolded` (weight 3, districts []). `pick_plan`: widest plan with width >= 8. `apply_plans`: `plan.rare = id`. `build`: scaffold over floors 1..3 (y `y0 + 4.6` to `y0 + 14.2`): standards (vertical tubes `(0.05, 9.6, 0.05)`) every 2.0 m along u at x = `side_sign * 8.3` and `side_sign * 8.75` (two rows, 0.45 apart), ledgers (horizontal tubes along z, `(0.05, 0.05, span)`) at every floor line joining the standards, transoms `(0.5, 0.05, 0.05)` joining the rows, planks `(0.5, 0.04, span)` on each level, all iron / `prop_material(&"plank", Color(0.45, 0.35, 0.2), 0.95, 0.0)`, meshes "ScaffoldTubes" + "ScaffoldPlanks"; a green net: box `(0.01, 9.6, span)` at x `side_sign * 8.2` with `prop_material(&"net", Color(0.2, 0.5, 0.3, 0.55), 0.9, 0.0)` and `transparency = BaseMaterial3D.TRANSPARENCY_ALPHA` (set on that cached material), mesh "ScaffoldNet", shadows OFF. Everything at abs(x) >= 7.95 (passes the lane box) and gated anyway.

# `.tres` files
One per piece in `resources/facades/set_pieces/` following the existing files' syntax exactly (script ext_resource + exported values).

# Do not touch
Shaders (`facade_sign.gdshader` may NOT change), `facade_body.gd`, `facade_keep_out.gd`, `facade_plan.gd`, `corridor_segment.*`, `travel_*.gd`, `van.tscn`, balance/export files, baselines.

# Verification
`py -3 tools/check.py` clean; `py -3 tools/smoke.py` passes (fingerprint unchanged; `bay mouth clear:` logged). One-off headless sanity script outside the repo, like the one from step 8a: for each new piece id find a seed (1..600) whose `rare_id()` matches after `configure(seed, d, 7, true)` for a district that allows it, print the seed, the node names added, and for spans confirm `Facades/Span` exists; then `open_bay` on that side (or `&"left"` for spans) and assert the rare is gone. Report check tail, smoke tail (30 lines), sanity output, `wc -l` of every script created or edited, `git status --short`. Do not commit.
