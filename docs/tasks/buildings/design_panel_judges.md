================================================================================
JUDGE visual-director winner: content-first
 - shader-first: total 38 (vis 9 feas 5 rob 8 var 8 fit 8) :: Highest surface ceiling: 7 wall modes, SDF window grid with arches, lit/boarded/broken panes, halo, fake normal map, trims silhouette mesh, 8 districts, 16 set-pieces. Verified in Godot 4.7 headless: CUSTOM0/CUSTOM1 vertex attributes via SurfaceTool.set_custom_format compile and the format bits are set automatically (the 'wrong flag silently zeros' risk only applies to add_surface_from_arrays); 'v
 - geometry-first: total 33 (vis 7 feas 7 rob 6 var 6 fit 7) :: Real reveals, sills, piers and cornices would catch the van's raking lights better than any normal map, and MultiMesh windows with INSTANCE_CUSTOM are verified: INSTANCE_CUSTOM compiles in the spatial vertex stage and must be passed to fragment through a varying (reading it in fragment() fails to compile), which the design's wording glosses over. SurfaceTool.create_from_arrays and generate_tangent
 - content-first: total 40 (vis 9 feas 6 rob 7 var 10 fit 8) :: The only design built around what a 2.5 s, 56 m-fog street can actually deliver: silhouette (parcel heights + backdrop quad), light (lit fraction per district + up to 2 sodium/mercury/pink pools per tile) and a readable word (Image-drawn block-glyph signs). Verified: shop_booth_flyers.glyph_pattern (line 315) has A B C D E F H I K L N O P R S T U W Y only, so the claimed missing G J M Q V X Z is e
 - robustness-first: total 37 (vis 5 feas 9 rob 9 var 5 fit 9) :: The safest and most rule-conformant: every placement goes through ctx.add_box against pre-pushed keep-outs (LANE, RAIDER_MARGIN, MOUTH, PARK_SWEEP), container hiding replaces the name-prefix rule, host_segment meta, free() instead of queue_free on rebuild, a ~300-build headless stress matrix, and the light counter. Verified: 'instance uniform' plus model-space VERTEX mapping compiles in 4.7 and se
 GRAFT:
   * From robustness-first: route every emitter, prop, sign and set-piece box through one FacadeContext.add_box that refuses on keep-out overlap and counts rejects, with the keep-outs pushed before any placer runs; add the PARK_SWEEP volume (no sidewalk props anywhere on the bay side of the host tile, x 7.25..9.0, y < 4.0) because the reverse-park arc (radius 6 then a 2 m lateral run to DockPoint x 8) sweeps the sidewalk beyond the mouth's z range.
   * From robustness-first: tc._active_stop.set_meta(&"host_segment", host_segment) in place_bay_stop so the smoke assertion finds the host without touching travel_controller.gd; free() rather than queue_free() on the old facade container during a same-frame rebuild; a static light counter decremented on tree_exited; a headless 'facade stress' matrix (every district x every set-piece x every mode on both sides) run from the smoke test.
   * From shader-first: carry per-building mode, palette index, building seed and lit fraction in CUSTOM0/CUSTOM1 vertex attributes (verified to compile and to survive SurfaceTool.commit) instead of only COLOR/UV2, so one ShaderMaterial per district covers brick, precast, corrugated and stone parcels in the same mesh; add the window halo term so lit panes read without enabling Environment glow; force the mouth building to setback 0 with two flank returns to x 14.4 at z +-4.6 and a soffit at y 8.2 so the bay reads as the ground floor of a real building rather than a hole beside void.
   * From shader-first: the metric-UV convention (UV.x along travel, UV.y height above road) applied to junction spans built as quads by the same builder, replacing the BoxMesh uv_scale trick; a corner tower/pilaster at each (+-9, +-9) junction meet; the 'no set-piece within N tiles of a junction' rule read from aligned_special_progress.
   * From geometry-first: the tools/facade_svg.py elevation preview generated from the headless dump so the owner can see compositions per district without a window; visibility_range_end 64 on every facade GeometryInstance3D so only the ~7 tiles inside fog render; MultiMesh for repeated node props (AC units, downpipes, blade signs) with cast_shadow OFF; the min_segment_index / cooldown fields on FacadeSetPiece.
   * From geometry-first: real 0.45 m window reveals and a 0.25 m pier at parcel boundaries on the ground and first storeys only (where the van's own lights rake the wall), keeping upper storeys as shader windows.
 FIX:
   * BoxMesh UV claim is false: Godot 4.7 BoxMesh lays its six faces out in a 2x3 UV atlas (probe: +X face u 0.333..0.667, v 0..0.5), so 'uv_scale = face size' on junction and side-street-branch BoxMesh walls will not turn 0..1 into metres. Either build those walls as facade quads with metric UVs (the same builder with a span length argument) or de-atlas per face in the shader from the flat normal; the map's 'BoxMesh UVs run 0..1 per face' line is wrong too.
   * Elevated rail and any other SPAN piece with pier columns reaching the ground (columns 0.6 m at x +-8.2) can stand over the DockPoint (x 8, z 0) in front of a garage because SPAN pieces survive the BAY rebuild. Rule: a SPAN piece may own no geometry below y 9; anything grounded is SIDE-tagged and dropped by the BAY rebuild. Tower crane and radio mast must also be excluded from the junction-aligned tile.
   * BAY rebuild keeps sidewalk props (newspaper box, vending machine, phone booth, dumpster, shrine, barrel fire at x 7.7..8.8) for |z| 4.9..10 on the bay side; the reverse-park arc (STOP_PARK_TURN_RADIUS 6, then dock_lat - 6 = 2 m lateral to x 8) swings the van's tail corners over that sidewalk. Add a PARK_SWEEP keep-out (bay side of the host tile, x 7.25..9.0, y < 4.0, full z) and drop every sidewalk prop and awning on that side.
   * Mouth numbers: the doc says vestibule slab top 8.15; the real ceiling is 0.35 thick centred at 7.8 (top 7.975) and the side walls' outer faces are at z +-4.5. Keep the generous 8.4 / +-4.9 volume but state the real figures, and derive the volume from StopVestibule.DOOR_X / MOUTH_WIDTH / MOUTH_HEIGHT constants instead of copying them.
   * Set-piece eligibility ignores junction-aligned tiles: a 45 m radio mast, crane jib or church tower next to a T-junction or crossroads clips the junction walls. Read tc._next_segment_progress / aligned_special_progress and skip set-pieces within one tile of a junction.
   * Light budget is per tile (2) with 12 alive tiles, i.e. up to 24 OmniLights plus tweened burning-building lights; cap facade lights world-wide (e.g. 6, counted through a group) and give every facade light visibility_range_end ~60 and shadow_enabled false, since the van's three world lights are already shadowed.
   * Scope and caps: 17 steps, 20 set-piece scripts, GPUParticles3D for smoke and steam set up blind. Cut the first pass to the storefront vocabulary, signs, lamps and five set-pieces (billboard, water tower, mural, scaffold wrap, radio mast), keep particles out of v1, and pin facade_builder (280), facade_ground_floor (260), facade_props (250) with explicit function lists so none crosses 300; corridor_segment.gd at ~230 must not absorb any builder logic.
   * Path error: corridor_segment_near_progress lives on travel_world.gd (line 88), not tc; the smoke assertion should use the host_segment meta on the active stop rather than a new tc.get_stop_host().
   * The 'card_bias' hook on Dead Zone reads act/card state into the world; invariant 12 makes that the owner's call, so leave it out of the plan entirely rather than 'optional later'.
   * Every emissive window and sign relies on EMISSION with no glow pass; adopt shader-first's halo term in the wall shader now so lit panes read at 30-40 m through the depth fog, and keep Environment glow strictly as the owner's opt-in step (it re-blesses the scene dump and blooms the van interior).
================================================================================
JUDGE gdscript-engineer winner: geometry-first
 - shader-first: total 34 (vis 8 feas 4 rob 6 var 9 fit 7) :: Highest ceiling (7 wall modes, halo, arches, 8 districts, 16 pieces, real skyline) but almost all of it lives in a ~280-line procedural shader plus CUSTOM0/CUSTOM1 vertex attributes that nobody can look at; a spec-driven implementer will get it compiling (verified: headless prints SHADER ERROR, custom formats and varying flat work) but not looking right. Concrete defects: the smoke mouth assertion
 - geometry-first: total 38 (vis 8 feas 6 rob 8 var 8 fit 8) :: Real reveals, piers and cornices catch the van's three shadowed lights, which flat normal-mapped plates cannot; MultiMesh windows with emission; 20-36 m skyline; overhead variants retained so Variant0/2/3 content survives. Per-plot ArrayMeshes plus per-instance AABBs make the mouth audit exact (verified: strict AABB.intersects means a mouth plot starting at v 8.0 and flank plots at |u| ≥ 4.6 do no
 - content-first: total 34 (vis 8 feas 5 rob 5 var 10 fit 6) :: Best content thinking: readable words via BlockGlyphs (glyph_pattern is static at shop_booth_flyers.gd:315, so the move is safe and flyer RNG order is untouched), light pools, 10 storefront units, 20 set-pieces, ASCII probe, stateless stride eligibility, the no-open-roll-up rule. But the keep-out section contradicts the vocabulary: ROAD_SPAN (|x| < 8.3, y < 9) forbids awnings 0.9 m out at y 3.2, b
 - robustness-first: total 36 (vis 5 feas 8 rob 8 var 6 fit 9) :: Most mechanical to implement from specs: BoxMesh plates with model-space VERTEX mapping via instance uniforms (verified to compile; immune to the BoxMesh atlas-UV problem and to tile rotation), MultiMesh sills/cornices, every placement through ctx.add_box against LANE/RAIDER_MARGIN/MOUTH/PARK_SWEEP, per-node AABB audit that works because everything is a box, host_segment meta on the stop, free() n
 GRAFT:
   * robustness-first: PARK_SWEEP (bay side of the host tile, x 7.25..9, y < 4) and RAIDER_MARGIN keep-outs added to facade_keepout.gd, since the reverse-park arc (STOP_PARK_TURN_RADIUS 6, REVERSE_STRAIGHT 8) brushes the sidewalk beyond the mouth.
   * robustness-first: tc._active_stop.set_meta(&"host_segment", host_segment) in place_bay_stop so the smoke mouth assertion finds the host without touching travel_controller.gd; the vestibule is parented to tc.corridor_root, not the host, so walking under the host never sees it (verified).
   * robustness-first: free() rather than queue_free() on the old facade container during rebuild(true), so an audit in the same frame never sees dying nodes; and a live audit from the earliest step (mouth check at step 3 when facades first exist; the lane check only once Variant2/3 pillars and thin pipes are gone).
   * robustness-first: model-space VERTEX mapping with instance uniform map_offset for any BoxMesh wall that keeps its primitive (junction stems, branch flanks) instead of UV tricks, because BoxMesh UVs are a 3x2 atlas (verified), and a static FacadeAudit.active_lights counter decremented in tree_exited for the world-wide light cap.
   * robustness-first: hide by container, never by name prefix: move the six Variant3 pillars under Variant3/Left and Variant3/Right containers toggled by side instead of renaming them to satisfy _is_side_structure_node, then delete the prefix functions.
   * shader-first: the emission halo term (halo_strength * lit * smoothstep around the pane) in facade_window/facade_wall so lit windows read at 30-45 m without enabling Environment glow; per-subsystem RNG salts (plan/props/signs/set-piece) off one tile seed; min_segment_index per set-piece; facade_plan.validate() that push_errors keep-out violations so the smoke run fails on them; a warm_up() pass in IDLE that draws one hidden triangle per cached material to absorb the Forward+ shader-compile hitch.
   * shader-first: facade dump as text (plots, families, props, keep-out AABBs per tile) behind the console, which is the only preview a windowless session can read, alongside geometry-first's SVG.
   * content-first: BlockGlyphs word signs (move glyph_pattern out of shop_booth_flyers.gd, keep it a one-line delegate so flyer RNG order is untouched) with per-district word lists on fascia, blade and rooftop signs; a readable word is the most memorable thing a 2.5 s tile can carry.
   * content-first: the rule that no roll-up is ever open on a non-stop tile (loading docks closed, raised, unlit), the one-GPUParticles3D-per-tile cap, and stateless set-piece eligibility ((segment_index + run_seed) % stride) so no cooldown state lives on tc.
   * content-first: the ASCII elevation probe over every district x side x mode x N seeds as a separate tool, kept out of the 300 s smoke budget.
 FIX:
   * INSTANCE_CUSTOM is a vertex-only built-in in 4.7 spatial shaders (verified: fragment use is 'Unknown identifier'); facade_window.gdshader must copy it to a varying (flat) in vertex() before reading lit/warmth/hash/mode in fragment().
   * Keep-out rules contradict three recipes: Collapsed Corner rubble at |x| ≥ 7.9 and Scaffold Wrap poles at d = -0.8 (x 8.0) both land inside LOW_PROTRUSION (|x| < 8.4, y < 4); either move rubble/poles to |x| ≥ 8.4 or split the rule into a lane box (|x| < 7.6, y < 2.5) and a sidewalk box that permits ≤ 0.9 m tall clutter at 7.9..8.8 on non-bay sides.
   * Mouth-plot boundary piers: a pilaster centred on u = ±4.6 overlaps MOUTH z ±4.6; the plotter must start flank-plot piers at |u| ≥ 4.6 (pier 4.6..4.85) and the mouth plot must emit nothing below v 8.0, including party caps and end caps; add this case to the dump tool.
   * The optional branch facade must not rely on side_street_branch._ready mirroring: _ready runs when the tile is instantiated (travel_world.gd:52), before dress(), and negative-basis mirroring flips winding (the branch material disables culling for exactly that reason). Build branch facades with a sign flag in frontage space, never negative scale, as robustness-first specifies.
   * dress() runs after apply_side_streets in spawn_world_segment, and _set_side_street(side, true) calls facade.clear() on a facade that was never built; dress() must build only sides whose state is CLOSED and open_bay/side-street toggles must tolerate an unbuilt facade.
   * The smoke assertion must use collect_global_aabbs() (per plot mesh, per MultiMesh instance, per set-piece node) and never a MultiMeshInstance3D's own AABB, which spans the whole side; exclude RoadFloor and SideStreetCorners, and note that the vestibule is under corridor_root so it is not walked.
   * facade_dump.gd must add each corridor_segment.tscn instance to the tree (the tile's @onready refs and RoadFloor._ready need it), unlike scene_dump; set the two side_street_branch instances hidden and skip their RoadFloor rebuilds or 168+ builds will be slow.
   * Do not keep the name-prefix hide rule alive by renaming PillarL/R to LeftPillar/RightPillar; put the pillars under per-side containers and delete _is_side_structure_node, or the map's 'new node covers a garage entrance' pitfall survives the overhaul.
   * Stop citing iron_cross.gd:314 for the left-side winding trick (its _add_quad has no swap); spec the winding explicitly: Godot front faces are clockwise, emit (a,b,c,d) on the right and (a,d,c,b) on the left, set normals before generate_tangents().
   * The tc edits (SEGMENT_VARIANT_COUNT to DISTRICT_COUNT 7, NEIGHBORHOOD_MAX_LENGTH 5 to 8) and VARIANT_COUNT 4 to 3 land in the over-cap travel_controller.gd and corridor_segment.gd in one commit (step 6) with a git grep proof; the design lists them in different places.
   * Lights: cap is per side but 12 alive tiles times (2 + set-piece) lights is up to ~30 shadowless OmniLights; add a world-wide counter (robustness-first's static active_lights) and keep light_cull_mask on layer 1 so facade lamps never light the van interior (layer 2).
   * The mouth rebuild re-plans the whole side from the same seed, so a side the player already saw at 20-40 m changes; make the plotter deterministic enough that only the mouth plot and its two neighbours differ (fix plot boundaries first, then carve), and forbid_on_bay_tile on every piece taller than a storey.
================================================================================
JUDGE qa-lead winner: geometry-first
 - shader-first: total 33 (vis 8 feas 4 rob 5 var 9 fit 7) :: Ambitious look (7 surface modes, setback jogs, halo term, trims mesh, 16 set-pieces) but ~440 lines of blind GLSL with arch math, height_at normals and CUSTOM0/1 encoding that only the parser can check. Verified: SurfaceTool.set_custom_format sets the ARRAY_FORMAT_CUSTOM0/1 bits itself (the 'silently zeros' risk is overstated); CUSTOM0 is vertex-only so the varying flat plan is right; uniform vec3
 - geometry-first: total 36 (vis 9 feas 6 rob 7 var 7 fit 7) :: Only design with real depth: 0.45 m reveals, piers, cornices, parapets, 6-13 storey skyline, MultiMesh windows with five modes, one sodium light per side, distance fade. Per-plot meshes make the headless mouth AABB test exact (verified MeshInstance3D.get_aabb works on the dummy renderer). MOUTH x 8.0..15, y -0.4..8.0, z +-4.6 matches the vestibule (walls x 9..14.6, z 4.1..4.5, ceiling slab centred
 - content-first: total 32 (vis 8 feas 4 rob 4 var 10 fit 6) :: Readable words on signs (BlockGlyphs from shop_booth_flyers.glyph_pattern, which is a static func, verified) is the single most memorable idea here; the storefront vocabulary and 20 set-pieces are the widest catalogue. But the scope is 17 steps, ~1300 lines of builders plus 20 subclass scripts, particles and tweens, all blind. Verified wrong: BoxMesh UVs are a 3x2 atlas (each face 1/3 x 1/2 of UV 
 - robustness-first: total 35 (vis 5 feas 8 rob 8 var 5 fit 9) :: Cleanest engineering: one add_box/is_clear gate with a rejected counter, four keep-outs including PARK_SWEEP, container-based hiding with no prefix rules, cached materials, instance uniforms for per-tile mapping (verified to parse; project is Forward+), BayBody piers/lintel/hood that close the void above and beside the vestibule, and a facade stress matrix over every district x piece x mode. Every
 GRAFT:
   * robustness-first: route every quad, MultiMesh instance, collider and set-piece box through one FacadeContext.add_box/is_clear gate with a `rejected` counter, and add its PARK_SWEEP keep-out (bay side, |x| 7.25..9.0, y < 4.0, whole tile z) on top of geometry-first's three boxes.
   * robustness-first: the `facade stress` matrix (every district x every set-piece forced x every left/right CLOSED/STREET/BAY combination, ~300 offscreen builds) called from smoke_route before the first fork, returning `OK ...` or FAIL lines, so the mouth guarantee is proven for districts the smoke run never drives through.
   * robustness-first: delete Structure entirely and hide by container (Facades/Left|Right freed and rebuilt), never by name prefix; put the surviving overheads (Variant0 beams, Variant2 cross pipes, Variant3 truss/catwalk) into the builder as district `overhead` recipes that pass the keep-out check, dropping the lane-violating pillars and rails.
   * robustness-first: `tc._active_stop.set_meta(&"host_segment", host_segment)` in place_bay_stop (the vestibule is not a child of the tile), `free()` instead of `queue_free()` on the old container so the audit never sees dying nodes, and BayBody-style piers plus a hood block above the vestibule ceiling so looking up inside the mouth shows building, not void.
   * robustness-first: model-space mapping with `instance uniform vec2 map_offset` for the junction and branch BoxMesh walls, which sidesteps the verified 3x2 BoxMesh UV atlas instead of trying to scale 0..1 UVs.
   * shader-first: the height-banded protrusion table (flush below y 3.0, <= 1.2 m between 3.0 and 4.6, <= 1.6 m above) and per-building BoxShape3D colliders at the plot face, so ricochets follow the visible wall; the window halo term (`halo_strength * lit * smoothstep(0.45, 0.0, edge)`) so lit windows read without enabling Environment glow; `facade_materials.warm_up()` in IDLE to hide the per-district shader-compile hitch; counting set-piece lights through a `facade_lights` group.
   * shader-first: `facade dump <n>` printing the plan (plots, storeys, families, props, every keep-out AABB) as text, complementing geometry-first's SVG elevation for review-by-reading.
   * content-first: BlockGlyphs (move shop_booth_flyers.glyph_pattern into a static class, add the missing letters) and district word lists drawn onto cached emissive Image textures, so signs carry readable words; cap 3 signs per tile and cache by (word, fg, bg).
   * content-first: the explicit rule that no roll-up on a non-stop tile is ever open or lit (fake loading docks stay closed, raised and dark) and the explicit `ERROR: facade covers bay mouth: <names>` print in the smoke assertion, since push_warning never fails the run.
 FIX:
   * Build order covers side streets: spawn_world_segment calls apply_side_streets (which clears an open side) before districts.dress_segment builds both sides, so a side-street tile gets a facade in front of its branch. dress()/build() must read _left_open/_right_open and build STREET sides as cleared (or dress before apply_side_streets and let _set_side_street clear).
   * Keeping Variant0/2/3 with the renamed prefix hider contradicts the design's own keep-outs: Variant3 PillarL/R at x +-6.5 (y 0..18, verified at corridor_segment.tscn:357) and CatwalkRailL/R at +-6.8 are inside RAIDER_LANE and LOW_PROTRUSION, and CrossPipeLow crosses the road at y 5.5. Move overheads into the builder as keep-out-checked district recipes, drop pillars and rails, and delete _is_side_structure_node entirely.
   * collect_global_aabbs(), keepout_violations() and the facade dump must be computed from build-time records: under the headless renderer MultiMesh.get_aabb(), MultiMeshInstance3D.get_aabb(), get_instance_transform() and get_instance_custom_data() all return zeros (verified with Godot 4.7 --headless), so any readback-based check silently passes or misfires.
   * Host lookup for the smoke assertion is wrong: the vestibule is spawned by spawn_stop_host and positioned by global_transform, not parented under the host tile. Set a `host_segment` meta in place_bay_stop (or use corridor_segment_near_progress) and read tc._stop_bay_side.
   * facade_window.gdshader must read INSTANCE_CUSTOM in vertex() and hand it to fragment() through a varying; fragment access is a compile error (verified). Same for any CUSTOM0/UV2 use.
   * Branch flank facades are not mirrored by side_street_branch._ready: it runs when the tile is added (before apply_side_streets and dress), so anything built later lands unmirrored. Pass a sign/mirror flag into StreetFacade and flip x and winding in the builder.
   * _set_side_street(side, false) -> facade.rebuild(false) fires on every spawn (apply_side_streets runs for every tile) and again in place_bay_stop; make rebuild a no-op when the mode is unchanged or nothing is built yet, so a tile builds once at spawn and twice only on a bay.
   * facade_mesh.gd at ~300 lines (strips, reveals, piers, string courses, cornice, parapet, party caps, end caps, second trim surface, tangents) will overrun; split trims (cornice/parapet/piers/caps) into a second RefCounted helper up front so neither file approaches 400.
   * The MOUTH margin above the vestibule is 2.5 cm (lintel band at 8.0 vs ceiling top 7.975): spec the numbers exactly (MOUTH y max 8.0, band starts at 8.0) and add a hood block behind the band (x 9.2..15, y 8.0..12, z +-4.6) so nothing reads as void if the vestibule ever changes; also copy the vestibule constants from StopVestibule rather than retyping them.
   * Changing the neighbourhood run length to randi_range(4, 8) keeps the tc._rng call count but changes when the next neighbourhood draw happens relative to pick_side_streets draws, so side streets, stop picks and bay sides shift for a given run_seed; either keep randi_range(2, 5) or state plainly that fork/stop sequences change (the fingerprint does not read them, verified).
================================================================================
